def Main():
    # from PrintString import PrintString;

    # ps = PrintString();
    # ps.printName();

    # from Calculator import Calculator;
    # cal = Calculator();
    # cal.calculate();

    # from PythonVersion import PythonVersion;
    # ver = PythonVersion();
    # ver.version();

    # from MultiplicationTable import MultiplicationTable
    # mul = MultiplicationTable();
    # mul.multiplication(8);

    # from DisplayDate import DisplayDate;
    # dat = DisplayDate();
    # dat.date();

    # from AreaOfCircle import AreaOfCircle;
    # are = AreaOfCircle();
    # are.area(8);

    # from SwapString import SwapString
    # swap = SwapString();
    # swap.swap("Tinsae", "Getachew");

    # from AverageOfFourNumbers import AverageOfFourNumbers;
    # avg = AverageOfFourNumbers();
    # avg.average(10 , 15 , 20 , 30);

    # from SwapNumbers import SwapNumbers;
    # swap = SwapNumbers();
    # swap.swap(1, 2);

    # from AreaOfCircle import AreaOfCircle;
    # are = AreaOfCircle();
    # are.perimeter(7.5);

    from StringManipulation import StringManipulation
    str = StringManipulation()
    # str.generatTuplesAndList("3, 5, 7, 23");
    str.multiLineString()

    # from File import File;
    # fl = File();
    # fl.fileExtention("abc.java");

    # from ArrayList import ArrayList
    # al = ArrayList();
    # al.DisplayFirstAndLastElement(["Red", "Green", "White", "Black"]);

Main();
 
